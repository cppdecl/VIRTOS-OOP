#ifndef CONSTANTS_PATHS_HH
#define CONSTANTS_PATHS_HH

#include <string>

namespace Constants 
{
    inline std::string OSDir = "VIRTOS/";
    inline std::string UserBaseDir = OSDir + "users/";
    inline std::string UserDBPath = UserBaseDir + "users.db";
}

#endif // CONSTANTS_PATHS_HH
